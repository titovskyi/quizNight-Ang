import { SortGameDatePipe } from './sort-game-date.pipe';

describe('SortGameDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SortGameDatePipe();
    expect(pipe).toBeTruthy();
  });
});
