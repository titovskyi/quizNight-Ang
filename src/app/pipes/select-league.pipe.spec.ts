import { SelectLeaguePipe } from './select-league.pipe';

describe('SelectLeaguePipe', () => {
  it('create an instance', () => {
    const pipe = new SelectLeaguePipe();
    expect(pipe).toBeTruthy();
  });
});
